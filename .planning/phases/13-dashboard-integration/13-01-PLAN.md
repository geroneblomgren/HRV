---
phase: 13-dashboard-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/renderer.js
  - js/practice.js
  - index.html
  - styles.css
autonomous: true
requirements:
  - DASH-06

must_haves:
  truths:
    - "During a live practice session, three ring gauges are visible: Phase Lock, Coherence, and Neural Calm (when Muse-S connected)"
    - "Coherence gauge reads AppState.coherenceScore and updates smoothly with 0.08 interpolation"
    - "v1.2 sessions save meanCoherence to IndexedDB so the dashboard can display it"
  artifacts:
    - path: "js/renderer.js"
      provides: "drawCoherenceGauge() function, coherence canvas setup"
      contains: "drawCoherenceGauge"
    - path: "index.html"
      provides: "Third gauge canvas element in practice session layout"
      contains: "practice-coherence-gauge-canvas"
    - path: "styles.css"
      provides: "Absolute-centered positioning for coherence gauge"
      contains: ".coherence-gauge"
    - path: "js/practice.js"
      provides: "Coherence trace accumulation and meanCoherence in saved session"
      contains: "_coherenceTrace"
  key_links:
    - from: "js/renderer.js"
      to: "AppState.coherenceScore"
      via: "drawCoherenceGauge reads coherenceScore each frame"
      pattern: "AppState\\.coherenceScore"
    - from: "js/practice.js"
      to: "js/renderer.js"
      via: "startRendering() receives coherenceCanvas as 9th argument"
      pattern: "startRendering.*coherence"
---

<objective>
Add a coherence ring gauge to the live practice session UI and accumulate coherence data for session persistence.

Purpose: Restore coherence as a visible biofeedback dimension alongside phase lock, giving users three simultaneous metrics (brain state, HRV quality, breathing alignment). Also fixes the critical finding that v1.2 sessions don't save meanCoherence.

Output: Working coherence ring gauge in practice sessions + meanCoherence saved to IndexedDB for every v1.2 session.
</objective>

<execution_context>
@C:/Users/geron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dashboard-integration/13-CONTEXT.md
@.planning/phases/13-dashboard-integration/13-RESEARCH.md

<interfaces>
<!-- From js/renderer.js — existing gauge and rendering patterns -->
function drawNeuralCalmGauge()     // lines 562-676 — clone this for coherence gauge
function drawPhaseLockGauge()      // lines 417-554 — existing phase lock gauge
function _setupAllCanvases()       // line 912 — add coherence canvas setup here
function renderLoop()              // line 851 — add drawCoherenceGauge() call here
export function startRendering(waveformCanvas, spectrumCanvas, gaugeCanvas, pacerCanvas, sessionStartTime, sessionDuration, neuralCalmCanvas, eegCanvas) // line 874
export function stopRendering()    // line 929

// Module-level state pattern (from neural calm):
let _neuralCalmCanvas = null, _neuralCalmCtx = null;
let _displayedCalm = 0;

// Constants already defined in renderer.js:
const ZONE_THRESHOLDS = { aligning: 40, locked: 70 };
const ZONE_COLORS = { aligning: '#ef4444', locking: '#eab308', locked: '#22c55e' };

<!-- From js/practice.js — trace accumulation pattern -->
let _phaseLockTrace = [];        // line 19 — 1 per second
let _neuralCalmTrace = [];       // similar pattern
// In DSP tick interval (line 207):
_phaseLockTrace.push(AppState.phaseLockScore);
if (AppState.museConnected) _neuralCalmTrace.push(AppState.neuralCalm);

// _computeSummary() — lines 377-414
// _saveSession() — lines 676-708

<!-- From js/state.js -->
AppState.coherenceScore  // already populated live by DSP tick
AppState.calibrating     // true during first ~80s

<!-- From index.html — existing gauge layout in practice session -->
<!-- Lines 189-193: pacer-gauge (right) and neural-calm-gauge (left) inside .session-pacer -->
<div class="pacer-gauge" id="practice-pacer-gauge">
  <canvas id="practice-gauge-canvas"></canvas>
</div>
<div class="neural-calm-gauge" id="practice-neural-calm-gauge">
  <canvas id="practice-neural-calm-gauge-canvas"></canvas>
</div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coherence gauge canvas to HTML/CSS and drawCoherenceGauge() to renderer.js</name>
  <files>index.html, styles.css, js/renderer.js</files>
  <action>
**index.html:** Add a third gauge canvas element inside `#practice-session-viz` within `.session-pacer`, between the neural-calm-gauge and pacer-gauge divs (around line 192):
```html
<div class="coherence-gauge" id="practice-coherence-gauge">
  <canvas id="practice-coherence-gauge-canvas"></canvas>
</div>
```

**styles.css:** Add CSS for `.coherence-gauge` using absolute centering (matching the existing `.neural-calm-gauge` and `.pacer-gauge` pattern):
```css
.coherence-gauge {
  position: absolute;
  bottom: 8px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 3;
  width: 120px;
  height: 120px;
}
.coherence-gauge canvas {
  display: block;
  width: 100%;
  height: 100%;
}
```

**js/renderer.js:**
1. Add module-level state: `let _coherenceCanvas = null, _coherenceCtx = null; let _displayedCoherence = 0;`
2. Add `drawCoherenceGauge()` function — clone `drawNeuralCalmGauge()` with these changes:
   - Read `AppState.coherenceScore` instead of `AppState.neuralCalm`
   - Use `_displayedCoherence` for smooth interpolation with alpha `0.08`
   - Color: orange `#fb923c` (matching dashboard coherence series)
   - NO "Connect Muse-S" placeholder — coherence is always available when RR data flows
   - During calibration (`AppState.calibrating` is true): show calibration state mirroring the phase lock gauge's calibration display (progress bar based on elapsed time / 80s)
   - Zone thresholds: reuse existing `ZONE_THRESHOLDS` (aligning: 40, locked: 70)
   - Zone colors: use orange palette — dim orange for background ring `rgba(251, 146, 60, 0.15)`, bright `#fb923c` for filled arc
   - Zone labels: "Aligning" / "Locking" / "Locked" (same as phase lock)
   - Label below zone: "Coherence"
3. Update `startRendering()` signature: add 9th parameter `coherenceCanvas = null`:
   `export function startRendering(waveformCanvas, spectrumCanvas, gaugeCanvas, pacerCanvas, sessionStartTime, sessionDuration = 0, neuralCalmCanvas, eegCanvas, coherenceCanvas)`
   Set `_coherenceCanvas = coherenceCanvas || null;` and reset `_displayedCoherence = 0;`
4. Update `_setupAllCanvases()`: add `_coherenceCtx = _coherenceCanvas ? setupCanvas(_coherenceCanvas) : null;`
5. Update `renderLoop()`: add `drawCoherenceGauge();` after `drawNeuralCalmGauge();`
6. Update `stopRendering()`: add cleanup for `_coherenceCtx` and `_coherenceCanvas` (set to null)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('js/renderer.js','utf8'); const h=fs.readFileSync('index.html','utf8'); const c=fs.readFileSync('styles.css','utf8'); console.assert(r.includes('drawCoherenceGauge'), 'missing drawCoherenceGauge'); console.assert(r.includes('coherenceCanvas'), 'missing coherenceCanvas param'); console.assert(r.includes('_displayedCoherence'), 'missing _displayedCoherence'); console.assert(h.includes('practice-coherence-gauge-canvas'), 'missing canvas element'); console.assert(c.includes('.coherence-gauge'), 'missing CSS'); console.log('PASS');"</automated>
  </verify>
  <done>Coherence ring gauge renders during practice sessions using AppState.coherenceScore with orange color, smooth interpolation, zone labels, and calibration state. Canvas element is positioned center-bottom in the session layout.</done>
</task>

<task type="auto">
  <name>Task 2: Accumulate coherence trace, compute meanCoherence, save to session, wire startRendering</name>
  <files>js/practice.js</files>
  <action>
**js/practice.js:**

1. Add module-level variable alongside existing traces (around line 19):
   `let _coherenceTrace = [];`

2. Reset it alongside other traces in the session start function (around line 63 where `_phaseLockTrace = [];`):
   `_coherenceTrace = [];`

3. In the 1-second DSP tick interval (around line 207, after `_phaseLockTrace.push(...)`), add:
   `_coherenceTrace.push(AppState.coherenceScore);`
   This runs every second unconditionally (coherence is always computed from RR data, unlike Neural Calm which needs Muse-S).

4. Update `_computeSummary()` (around line 377) to include coherence metrics:
   After the neural calm summary block, add:
   ```js
   const cohTrace = _coherenceTrace.slice();
   let meanCoherence = null;
   if (cohTrace.length > 0) {
     meanCoherence = Math.round(cohTrace.reduce((a, v) => a + v, 0) / cohTrace.length);
   }
   ```
   Add `meanCoherence` to the return object.

5. Update `_saveSession()` (around line 676) to persist coherence:
   Add `meanCoherence: summary.meanCoherence,` to the saved session object (add it at the top level, unconditionally — it will be null for sessions shorter than calibration).

6. Wire the coherence canvas into `startRendering()` call (around line 188):
   Before the call, add: `const coherenceCanvas = _getEl('practice-coherence-gauge-canvas');`
   Append it as the 9th argument to `startRendering()`:
   ```js
   startRendering(
     waveformCanvas,
     null,
     gaugeCanvas,
     pacerCanvas,
     _sessionStart,
     _selectedDuration * 60,
     neuralCalmCanvas,
     eegCanvas,
     coherenceCanvas
   );
   ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('js/practice.js','utf8'); console.assert(p.includes('_coherenceTrace'), 'missing _coherenceTrace'); console.assert(p.includes('meanCoherence'), 'missing meanCoherence in summary/save'); console.assert(p.includes('coherenceCanvas'), 'missing coherenceCanvas wiring'); console.log('PASS');"</automated>
  </verify>
  <done>Coherence scores are accumulated once per second during sessions, meanCoherence is computed in summary, saved to IndexedDB, and the coherence gauge canvas is wired to the renderer.</done>
</task>

</tasks>

<verification>
1. `js/renderer.js` contains `drawCoherenceGauge()` that reads `AppState.coherenceScore`
2. `index.html` contains `practice-coherence-gauge-canvas` inside `.session-pacer`
3. `styles.css` positions `.coherence-gauge` centered at bottom
4. `js/practice.js` accumulates `_coherenceTrace`, computes `meanCoherence`, saves it, and passes coherence canvas to `startRendering()`
5. `startRendering()` accepts 9 parameters with coherenceCanvas as 9th
</verification>

<success_criteria>
- Three gauge canvases exist in the practice session HTML (neural calm left, coherence center, phase lock right)
- drawCoherenceGauge() follows the exact same ring gauge pattern as drawNeuralCalmGauge()
- Coherence data is accumulated and saved to every v1.2 session record
- Discovery tab continues to work without passing coherenceCanvas (defaults to null)
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-integration/13-01-SUMMARY.md`
</output>
