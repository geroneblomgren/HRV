---
phase: 11-phase-lock-engine
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified: []
autonomous: false
requirements:
  - LOCK-01
  - LOCK-02
  - LOCK-03
  - LOCK-04

must_haves:
  truths:
    - "Phase lock gauge shows 0-100 score with Low/Aligning/Locked zones during a live practice session"
    - "Score rises above 70 when breathing in sync with pacer for 30+ seconds"
    - "Score drops noticeably within 2 breath cycles when user pauses or shifts attention"
    - "Session summary shows Duration, Mean Phase Lock, Peak Phase Lock, Time Locked In"
    - "Session record in IndexedDB contains meanPhaseLock, peakPhaseLock, timeLockedIn fields"
  artifacts: []
  key_links: []
---

<objective>
Human verification that the phase lock engine works end-to-end during a live practice session.

Purpose: Validate all 4 LOCK requirements are met — phase computation produces meaningful scores, gauge displays correctly, summary shows phase lock metrics, and data persists.

Output: User confirmation or issue list for revision.
</objective>

<execution_context>
@C:/Users/geron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-phase-lock-engine/11-01-SUMMARY.md
@.planning/phases/11-phase-lock-engine/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase lock engine replacing coherence as the primary biofeedback metric:
- js/phaseLock.js: Hilbert-based phase lock score (0-100) computed every DSP tick
- js/renderer.js: Phase lock gauge with Low (<40) / Aligning (40-70) / Locked (70+) zones
- js/practice.js: Phase lock trace collection, summary computation, IndexedDB persistence
- index.html: Summary card labels updated to "Mean Phase Lock" / "Peak Phase Lock"
  </what-built>
  <how-to-verify>
1. **Connect HRM and start a practice session**
   - After clicking Start, observe the gauge area
   - It should show "Calibrating..." with a ~25-second countdown (NOT 120 seconds)
   - After ~25s, the phase lock gauge should appear with a score and zone label

2. **Breathe in sync with the pacer for 60 seconds**
   - The phase lock score should rise — ideally above 70 ("Locked" zone)
   - The gauge arc should fill and turn green in Locked zone
   - Score should feel responsive — rewarding sync within ~2 breath cycles

3. **Deliberately pause breathing or breathe out of rhythm for 15 seconds**
   - The score should drop noticeably (below 40 ideally)
   - The zone should change to "Low" or "Aligning"
   - Drop should be visible within 2 breath cycles (~10-20 seconds)

4. **Let the session complete (or stop it manually)**
   - Summary screen should show: Duration, Mean Phase Lock, Peak Phase Lock, Time Locked In
   - No mention of "Coherence" anywhere in the summary
   - Values should make sense (mean somewhere in 30-70 range, peak higher, time locked in > 0 if you synced)

5. **Verify IndexedDB persistence**
   - Open DevTools > Application > IndexedDB > resonanceHRV > sessions
   - Find the latest session record
   - It should have `meanPhaseLock`, `peakPhaseLock`, `timeLockedIn`, `phaseLockTrace` fields
   - It should NOT have `meanCoherence` or `coherenceTrace` fields

**Known acceptable behaviors:**
- Score may not reach exactly 100 even with perfect sync (phase offset is expected)
- Score may fluctuate ±10-15 points during steady breathing (FFT bin resolution noise)
- First 25 seconds showing 0 is expected (calibration)
  </how-to-verify>
  <resume-signal>Type "approved" if phase lock works correctly, or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
All 4 LOCK requirements verified through live session:
- LOCK-01: Phase lock score computed from FFT phase at pacing frequency
- LOCK-02: Score replaces coherence as primary metric — responsive to alignment
- LOCK-03: Gauge shows phase lock with Low/Aligning/Locked zones
- LOCK-04: Summary and IndexedDB use phase lock fields
</verification>

<success_criteria>
User confirms phase lock gauge, summary, and persistence work correctly during a live practice session.
</success_criteria>

<output>
After completion, create `.planning/phases/11-phase-lock-engine/11-03-SUMMARY.md`
</output>
