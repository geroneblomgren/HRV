---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - styles.css
  - manifest.json
  - sw.js
  - js/main.js
  - js/state.js
  - js/storage.js
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03

must_haves:
  truths:
    - "AppState is a reactive Proxy store that fires subscriber callbacks on property assignment"
    - "StorageService can save and retrieve sessions, settings, and Oura cache from IndexedDB"
    - "App shell renders dark-themed layout with top nav tabs and connection area"
    - "App is installable as a PWA with offline shell caching"
    - "AppState schema includes all fields for phases 1-5 with sensible defaults"
  artifacts:
    - path: "js/state.js"
      provides: "Proxy-based AppState with pub/sub (subscribe/unsubscribe)"
      exports: ["AppState", "subscribe", "unsubscribe"]
    - path: "js/storage.js"
      provides: "idb wrapper with sessions, settings, and oura object stores"
      exports: ["initStorage", "saveSession", "getSetting", "setSetting", "getOuraCache", "setOuraCache", "querySessions"]
    - path: "js/main.js"
      provides: "Bootstrap: init modules, register service worker"
      exports: ["default (IIFE)"]
    - path: "index.html"
      provides: "App shell with nav tabs, connection area, live data panel placeholders"
    - path: "styles.css"
      provides: "Dark theme CSS variables and base layout styles"
    - path: "manifest.json"
      provides: "PWA manifest for installability"
    - path: "sw.js"
      provides: "Cache-first service worker for app shell assets"
  key_links:
    - from: "js/main.js"
      to: "js/state.js"
      via: "ES module import"
      pattern: "import.*from.*state\\.js"
    - from: "js/main.js"
      to: "js/storage.js"
      via: "initStorage() call on app start"
      pattern: "initStorage"
    - from: "js/storage.js"
      to: "idb CDN"
      via: "ES module import from jsdelivr"
      pattern: "import.*from.*cdn\\.jsdelivr\\.net.*idb"
    - from: "js/main.js"
      to: "sw.js"
      via: "navigator.serviceWorker.register"
      pattern: "serviceWorker\\.register"
---

<objective>
Create the reactive AppState store, IndexedDB StorageService, and dark-themed PWA app shell that all subsequent phases build on.

Purpose: Every module in phases 2-5 reads from and writes to AppState, and persists data through StorageService. The app shell establishes the visual foundation (dark theme, nav tabs, connection area). This plan produces the central nervous system of the app.

Output: 7 files — js/state.js, js/storage.js, js/main.js, index.html, styles.css, manifest.json, sw.js — forming a working PWA shell with reactive state and IndexedDB persistence.
</objective>

<execution_context>
@C:/Users/geron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppState (Proxy reactive store) and StorageService (idb wrapper)</name>
  <files>js/state.js, js/storage.js</files>
  <action>
Create js/state.js implementing the Proxy-based reactive AppState exactly as specified in RESEARCH.md Pattern 1. The full schema MUST include all fields for phases 1-5 with sensible null/zero defaults. Export AppState, subscribe(key, fn), and unsubscribe(key, fn).

Fields to include (from RESEARCH.md Pattern 1):
- BLE state: connectionStatus ('disconnected'), connected (false), deviceName (null), connectionUptime (0), lastConnectTime (null), showManualReconnect (false)
- RR stream: rrBuffer (new Float32Array(512)), rrHead (0), rrCount (0), artifactCount (0), currentHR (0)
- Session state: sessionPhase ('idle'), sessionStartTime (null)
- DSP results (Phase 2 stubs): coherenceScore (0), lfPower (0), spectralBuffer (null), calibrating (true)
- Pacer (Phase 3 stubs): pacingFreq (0.0833), nextCueTime (0), nextCuePhase ('inhale')
- Settings: savedResonanceFreq (null), savedDeviceName (null)
- Oura (Phase 5 stubs): ouraData (null), ouraConnected (false)

Create js/storage.js implementing the StorageService as specified in RESEARCH.md Pattern 5. Import idb v8.0.3 from CDN: `import { openDB } from 'https://cdn.jsdelivr.net/npm/idb@8.0.3/+esm';`

Three object stores:
1. 'sessions' — autoIncrement id, keyPath 'id', index on 'timestamp'
2. 'settings' — out-of-line keys (string key names)
3. 'oura' — out-of-line keys (key = 'cache')

Export: initStorage(), saveSession(data), getSetting(key), setSetting(key, value), getOuraCache(), setOuraCache(data), querySessions({ limit }).

DB_NAME = 'resonancehrv', DB_VERSION = 1.
  </action>
  <verify>
    <automated>cd "C:/Users/geron/OneDrive/Desktop/AI Learning/AI Personal Projects/Sleep" && node -e "import('file://' + process.cwd().replace(/\\\\/g,'/') + '/js/state.js').then(m => { const keys = Object.keys(m.AppState); console.log('AppState fields:', keys.length); console.assert(keys.length >= 18, 'Missing fields'); console.assert(typeof m.subscribe === 'function', 'subscribe missing'); console.assert(typeof m.unsubscribe === 'function', 'unsubscribe missing'); console.log('PASS: state.js'); }).catch(e => { console.error('FAIL:', e.message); process.exit(1); })"</automated>
  </verify>
  <done>
    - js/state.js exports AppState (Proxy with 18+ fields), subscribe, unsubscribe
    - js/storage.js exports initStorage and all CRUD methods for sessions, settings, oura stores
    - AppState subscription fires callback when a property is set
    - StorageService creates 3 object stores on init
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PWA app shell — index.html, styles.css, manifest.json, sw.js, main.js</name>
  <files>index.html, styles.css, manifest.json, sw.js, js/main.js</files>
  <action>
Create index.html as the app entry point:
- DOCTYPE html, lang="en", meta viewport, meta theme-color #111111
- Link to styles.css and manifest.json
- Import map or ES module script tags for js/main.js (type="module")
- Top navigation: tabs for Discovery | Practice | Dashboard (per user decision). Only show tab structure; content areas are placeholders for later phases.
- Connection area: prominent "Connect to HRM 600" button (centered, visible on app open per user decision). Include a secondary label showing saved device name if available ("Connect to [device name]").
- Live data panel (always visible, per user decision): 4 cells — HR (BPM), Beats (rrCount), Artifacts (artifactCount), Uptime. Use the HTML structure from RESEARCH.md.
- Connection status banner area (subtle, top of page): shows "Reconnecting..." when status is reconnecting per user decision.
- Instructional error message area for pairing failures.

Create styles.css with dark theme:
- CSS custom properties from RESEARCH.md (--bg-primary: #0d0d0d, --bg-secondary: #1a1a1a, --bg-panel: #1e1e1e, --text-primary: #e8e8e8, --text-secondary: #888888, accent colors for states, font stacks)
- Body: dark background, no margin, font-family var(--font-ui)
- Nav tabs: horizontal, top of page, tab-style with active state
- Connection area: centered, prominent button styling
- Live data panel: fixed bottom or sidebar, always visible, grid of data cells with large value + small label
- Connection status banner: subtle, top position, color-coded (green connected, amber reconnecting, red disconnected)
- Responsive is NOT needed (desktop Chrome only, per out of scope)

Create manifest.json per RESEARCH.md Pattern 6 — name "ResonanceHRV", display standalone, dark background/theme colors. For icons: use simple inline SVG data URIs or placeholder paths (icons/icon-192.png, icons/icon-512.png).

Create sw.js per RESEARCH.md Pattern 6 — cache-first service worker. CACHE_NAME = 'resonancehrv-v1'. Cache all 7 shell assets: /, /index.html, /styles.css, /manifest.json, /js/main.js, /js/state.js, /js/storage.js. Do NOT cache /js/ble.js yet (Plan 02 creates it).

Create js/main.js:
- Import AppState and subscribe from js/state.js
- Import initStorage, getSetting from js/storage.js
- On DOMContentLoaded:
  1. Call initStorage()
  2. Load saved device name from settings, set AppState.savedDeviceName
  3. Load saved resonance frequency from settings, set AppState.savedResonanceFreq
  4. Register service worker (navigator.serviceWorker.register('/sw.js'))
  5. Wire UI subscriptions: subscribe to connectionStatus, currentHR, rrCount, artifactCount to update DOM elements
  6. Wire connection uptime timer: when connected, setInterval updates AppState.connectionUptime every second
  7. Wire nav tab click handlers (switch active tab, show/hide content areas)
  8. Set up Connect button click handler as a placeholder (will be wired to BLEService in Plan 02)
  - For now, the Connect button handler should log "BLEService not yet loaded" to console

Do NOT create icons/icon-192.png or icons/icon-512.png as actual image files — the manifest references them but they can be added later. The PWA will still function without icons on localhost.
  </action>
  <verify>
    <automated>cd "C:/Users/geron/OneDrive/Desktop/AI Learning/AI Personal Projects/Sleep" && node -e "const fs = require('fs'); const files = ['index.html','styles.css','manifest.json','sw.js','js/main.js','js/state.js','js/storage.js']; const missing = files.filter(f => !fs.existsSync(f)); if (missing.length) { console.error('MISSING:', missing); process.exit(1); } const html = fs.readFileSync('index.html','utf8'); console.assert(html.includes('Connect'), 'Missing connect button'); console.assert(html.includes('live-data-panel') || html.includes('live-panel'), 'Missing live data panel'); console.assert(html.includes('manifest.json'), 'Missing manifest link'); const css = fs.readFileSync('styles.css','utf8'); console.assert(css.includes('--bg-primary'), 'Missing CSS vars'); const mf = JSON.parse(fs.readFileSync('manifest.json','utf8')); console.assert(mf.display === 'standalone', 'Manifest not standalone'); const sw = fs.readFileSync('sw.js','utf8'); console.assert(sw.includes('resonancehrv'), 'SW missing cache name'); const main = fs.readFileSync('js/main.js','utf8'); console.assert(main.includes('initStorage'), 'main.js missing initStorage'); console.log('PASS: all shell files verified');"</automated>
  </verify>
  <done>
    - index.html loads with dark theme, nav tabs (Discovery/Practice/Dashboard), Connect button, live data panel
    - styles.css provides full dark theme with CSS custom properties and layout
    - manifest.json enables PWA installability
    - sw.js caches app shell assets with cache-first strategy
    - js/main.js bootstraps the app: initializes storage, loads saved settings into AppState, registers SW, wires UI subscriptions
    - Live data panel updates reactively when AppState properties change (verifiable by setting AppState.currentHR = 72 in console)
  </done>
</task>

</tasks>

<verification>
1. Open http://localhost:8080 (or whichever dev server) — dark themed app shell loads
2. Browser console: `AppState.currentHR = 72` — live data panel BPM updates to 72
3. Browser console: `AppState.connectionStatus = 'reconnecting'` — banner shows "Reconnecting..."
4. Browser DevTools > Application > IndexedDB — 'resonancehrv' database exists with sessions, settings, oura stores
5. Browser DevTools > Application > Service Workers — sw.js registered and active
6. Nav tabs switch between Discovery/Practice/Dashboard content areas
</verification>

<success_criteria>
- All 7 files exist and are syntactically valid
- AppState is reactive: setting a property fires subscribed callbacks
- StorageService initializes IndexedDB with 3 object stores
- App shell renders dark theme with nav tabs, connection area, live data panel
- Service worker registers and caches shell assets
- Saved settings (device name, resonance freq) load into AppState on startup
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
